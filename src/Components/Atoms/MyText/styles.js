import { StyleSheet } from 'react-native'
export default StyleSheet.create({
    text: (textAlign, fontSize, fontWeight, color, fontFamily, lineHeight) => ({
        textAlign,
        fontSize,
        fontWeight,
        color,
        fontFamily,
        lineHeight,
    })
})