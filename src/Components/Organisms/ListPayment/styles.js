import { Dimensions, StyleSheet } from 'react-native'

export default StyleSheet.create({
    container:{
        flex:1,
        paddingTop: 12,
    },
    emptyProduct:{
        flex: 1, 
        justifyContent: 'center', 
        alignItems: 'center',
        padding: 12
    }
})